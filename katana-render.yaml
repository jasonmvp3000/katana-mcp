services:
  - type: web
    name: katana-mcp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: KATANA_API_KEY
        sync: false
      - key: MCP_SECRET
        sync: false
      - key: NODE_ENV
        value: production
